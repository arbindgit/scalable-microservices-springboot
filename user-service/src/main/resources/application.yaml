server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/cowras?currentSchema=farmer_schema
    username: postgres
    password: 848210

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true